# Reporting Tool — Анализ обучения ОС ГД

Инструмент для анализа данных обучения сотрудников по образовательной системе Газпромнефть с подсчётом статистики по годам.

## Возможности

- Объединение нового файла выгрузки с накопительным итоговым файлом
- Автоматическое удаление дубликатов записей
- Классификация курсов по категориям: **BP** (бизнес-пользователи), **IT**, **ПРМ** (поддержка рабочих мест)
- Подсчёт уникальных обучающихся и человеко-курсов
- Статистика по годам (2022–2026): итоговая, накопительная, за отдельный год
- Выявление неклассифицированных курсов
- Экспорт отчёта в Excel

## Варианты запуска

### 1. Консольная версия (Python)

**Требования:** Python 3.10+, зависимости (см. ниже).

```bash
python для_ос_гд_с_подсчетом_за_2026_год.py
```

- Открывает диалоги выбора файлов
- Результаты выводит в консоль
- Автоматически сохраняет отчёт в Excel

### 2. GUI-версия (Python)

**Требования:** Python 3.10+, зависимости (см. ниже).

```bash
python для_ос_гд_с_подсчетом_за_2026_год_gui.py
```

- Полноценный графический интерфейс на customtkinter
- Прогресс-бар с отображением текущего этапа обработки
- Графики: столбчатая диаграмма по годам, круговая BP/IT, линия тренда
- Галочка «Перезаписать итоговый файл» — можно отключить перезапись
- Интерактивное управление неклассифицированными курсами:
  - Поиск по названию
  - Назначение каждого курса в категорию (BP / IT / PRM) или в блеклист
  - Пересчёт статистики после назначений
- Сохранение пользовательских назначений в `course_assignments.json`
- Вкладка «Назначения» — просмотр и удаление назначений
- Экспорт отчёта в Excel через диалог сохранения

### 3. Готовый .exe (без установки Python)

Скачайте архив `Анализ_обучения_ОСГД_GUI.rar` из раздела [Releases](../../releases) или соберите самостоятельно (см. раздел «Сборка»).

**Запуск:**

1. Распакуйте архив в любую папку
2. Запустите `Анализ_обучения_ОСГД_GUI.exe`
3. Python на компьютере **не требуется**

Архив содержит все необходимые зависимости. Работает на Windows 10/11 (64-bit).

## Установка зависимостей (для запуска из Python)

```bash
pip install pandas openpyxl customtkinter matplotlib
```

| Пакет | Назначение |
|---|---|
| `pandas` | Обработка данных |
| `openpyxl` | Чтение/запись Excel |
| `customtkinter` | GUI (только GUI-версия) |
| `matplotlib` | Графики (только GUI-версия) |

## Сборка .exe из исходников

Требуется PyInstaller:

```bash
pip install pyinstaller
```

**GUI-версия:**

```bash
pyinstaller -y --distpath dist_gui --workpath build_gui/work build_gui/gui.spec
```

**Консольная версия:**

```bash
build.bat
```

## Структура проекта

```
├── для_ос_гд_с_подсчетом_за_2026_год.py      # Консольная версия
├── для_ос_гд_с_подсчетом_за_2026_год_gui.py   # GUI-версия
├── courses.json                                 # Списки курсов (BP, IT, PRM)
├── course_assignments.json                      # Пользовательские назначения (создаётся автоматически)
├── build.bat                                    # Сборка консольной версии в .exe
├── build_gui/gui.spec                           # Спецификация PyInstaller для GUI
├── README.md
└── .gitignore
```

## Формат входных данных

Ожидается Excel-файл (`.xlsx`) со столбцами:

| Столбец | Описание |
|---|---|
| Название | Название курса |
| Статус | Статус прохождения |
| Дата начала | Дата начала обучения |
| Дата завершения | Дата завершения обучения |
| ФИО участника | ФИО обучающегося |
| Подразделение | Подразделение сотрудника |
| Должность | Должность сотрудника |
| Предприятие сотрудника | Предприятие |
